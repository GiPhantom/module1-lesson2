stages:
  - test
  - build
  - test2
  - stage
  - prod
  
install_env:
   stage: test
   script:
     - echo " test 1------------------------- "
   tags:
     - stage-shell

run tests:
   stage: test
   script:
     - echo " test 2------------------------- "
   tags:
     - stage-shell

linters:
   stage: test
   script:
     - echo " test linter--------------------- "
   tags:
     - stage-shell

build:
   stage: build
   script:
#     - ./build.sh
    - echo 'running ./build.sh' 
#  when: manual
   tags:
     - stage-shell

tests_after_build:
   stage: test2
   script:
     - echo " tests_after_build 1--------------------- "
   tags:
     - stage-shell

deploy website:
   stage: stage
   script:
     - cp -rf ./html/* /var/www/html
#     - cp -rf ./html/* /var/www/html
     #- ./deploy.sh
    - echo 'running ./build.sh'
   when: manual
   tags:
     - stage-shell
   only:
     refs:
      - tags
      - master
      - /^release.*$/
      
    
deploy prod:
   stage: prod
   script:
#     - ./deploy.sh
      - echo 'running ./build.sh'
   when: manual
   tags:
     - stage-shell
   only:
     refs:
      - tags
      - master
      - /^release.*$/




